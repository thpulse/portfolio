<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kitri.perform.user">

	<insert id="insert" parameterType="user">
     	insert into userinfo values(#{user_id}
     								,#{user_pass}
     								,#{user_pass2}
     								,#{user_name}
     								,#{user_ssn}
     								,#{user_tel}
     								,#{user_email}
     								,#{user_gender}
     								,#{user_gerne}
     								,1
     								,'ROLE_USER')
     					
    </insert>
    
    <select id="login" resultType="user" parameterType="Map">
    	select * from userinfo where user_id=#{id} and user_pass=#{pass}
    </select>
    
    <select id="list" resultType="user">
    	select * from userinfo where del_flg = 1
    </select>
    
    <update id="delete" parameterType="String">
    	update userinfo set del_flg =0 where user_id = #{user_id}
    </update>
    
    <select id="securityLogin" resultType="map">
    	select user_id as username,
    		   user_pass as password,
    		   user_pass2,
    		   user_name,
    		   user_ssn,
    		   user_tel,
    		   user_email,
    		   user_gender,
    		   user_gerne,
    		   del_flg,
    		   role as authority
    		   from userinfo where user_id = #{username}
    
    </select>
    
     <select id="idsearch" resultType="String" parameterType="Map">
    	select user_id from userinfo 
    			where user_name=#{user_name} and user_tel=#{user_tel}
    
    </select>
	
	<select id="passsearch" resultType="String" parameterType="Map">
		select user_pass2 from userinfo
				where user_id=#{user_id} and user_ssn=#{user_ssn}
	</select>

</mapper>