<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kitri.perform.Rcheck">
	<select id="list" resultType="Rcheck" parameterType="String">
		SELECT r.reserv_date, r.prf_day, r.prf_starttime, p.prfnm FROM performance p, reservation r WHERE r.prf_id = p.prfid and r.user_id = #{userid}
	</select>
</mapper>











